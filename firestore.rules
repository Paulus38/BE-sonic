rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Clients do not access Firestore directly.
    // NestJS Admin SDK bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
